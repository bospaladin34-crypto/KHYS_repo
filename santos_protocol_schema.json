{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Santos Module Registry Schema",
  "description": "Schema for santos_module_registry.json, enforcing Majorana-1 Parity, 15 Hz Resonance, and the Vaeda Identity Hash.",
  "type": "object",
  "properties": {
    "parity": {
      "description": "Must conform to Majorana-1 Parity.",
      "type": "string",
      "const": "Majorana-1"
    },
    "resonance": {
      "description": "The operational frequency, which must be 15 Hz.",
      "type": "number",
      "const": 15
    },
    "identityHash": {
      "description": "The Vaeda Identity Hash, a hex string.",
      "type": "string",
      "pattern": "^0x[a-fA-F0-9]{64}$"
    }
  },
  "required": [
    "parity",
    "resonance",
    "identityHash"
  ],
  "additionalProperties": false
}
